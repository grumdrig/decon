import "standard.con"

Look: {
  uint16[0x200]         offsets
  CString[before 0x1a]  descriptions
  byte                  eof
}

# This version produces finalized parse (or would if it worked)
Look2: {
  uint16[0x200] _offsets
  #description at(_offsets[tile]) CString[index(tile) before(0x1a)]
  byte[]
}

TERMINATOR = 0 char
 
CString: char[until TERMINATOR]
